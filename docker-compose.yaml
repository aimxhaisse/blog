services:
  dev:
    image: hugomods/hugo:latest
    command: hugo server --bind 0.0.0.0 --port 1313
    volumes:
      - .:/src
    ports:
      - "1313:1313"

  prod:
    build: .
    ports:
      - "8080:80"
